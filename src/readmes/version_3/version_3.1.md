□ Design comments table = done

□ Create Drizzle schema = done

□ Repository = done

□ Service = done

□ Controller = done

□ Routes = 4 / 4

□ Validators = done

□ CRUD APIs = done

□ Activity Integration = done

□ Soft Delete = done

□ Pagination 